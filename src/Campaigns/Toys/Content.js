import React from 'react';

const Content = () => {
  return (
    <div>

    </div>
  );
}
 
export default Content;


// Para dispositivos movil: descripción fecha, hora y lugar.

// Para tablet: elementos anteriores + cuenta regresiva.

// Para pc de escritorio: elementos anteriores + imágenes alrededor del contenido principal.

// Para smart tv: elementos anteriores + dar color al fondo con transparencia.